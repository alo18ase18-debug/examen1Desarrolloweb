CREATE TABLE IF NOT EXISTS aeropuertos (
                                           id      INT          PRIMARY KEY,
                                           nombre  VARCHAR(200),
    ciudad  VARCHAR(100),
    pais    VARCHAR(100),
    codigo  VARCHAR(10)
    );